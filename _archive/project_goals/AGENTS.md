# Archived Project Goals Rules

本目录保存废弃的历史 `项目目标/` 内容，包括旧路线、旧任务包、旧 handoff 和历史证据。

## 规则

- 默认只读。
- 不按本目录里的旧计划直接执行，除非用户明确指定。
- 如需引用历史原因，必须说明它是历史上下文，不是当前执行口径。
- 新的路线、进度和决策记录应写到 `../../docs/project_management/`，不要写回本目录。
