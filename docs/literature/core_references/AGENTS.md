# Core References Rules

本目录保存支撑当前论文主线的核心文献。

## 规则

- 只放与当前研究主线直接相关的文献，例如 EnergyPlus 数据中心模型、TES、冷却系统、PV、电价、MPC 或控制建模。
- 新增文献时应能说明它支撑论文哪一部分。
- 不把泛泛背景文献放入本目录；背景材料应放入 `../other_references/`。
- 如果某篇文献会进入 `references.bib`，应保持文件与 citation key 可追溯。
- 解析或摘要文献时，默认使用显式 GPT-5.5 subagent，并返回来源、核心结论和与当前项目的关系。
