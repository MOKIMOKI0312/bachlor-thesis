# 核心公式、伪代码和算法

## 抽取边界

- 来源 PDF：`2022_Wang_Toward_Physics_Guided_Safe_Deep_Reinforcement_Learning_for_Green_Data_Ce.pdf`
- 标题：Toward Physics-Guided Safe Deep Reinforcement Learning for Green Data Center Cooling Control
- 抽取重点：公式、优化目标、约束、MPC/MILP/控制流程、输入输出变量。
- 注意：PDF 公式转文本可能丢失上下标、希腊字母、分式结构和跨栏顺序；这里是低 token 检索索引，不是最终论文公式稿。

## 人工核对页线索

自动抽取；未做人工页码核对。

无人工补充说明。

## 算法/控制流程候选

### Page 3

Policy learning Requirements for safety Safety implementation Approach Studies Applications category Exploratory data Transition model When? Explicitness DRL (simplex) [19, 24] Load balancing, etc. Explicit Required Not required Reactive DRL (reward shaping) [9, 17, 25, 28] DC cooling, etc. Semi-explicit Model-free* † DRL (post-hoc [8, 11] HVAC control, etc. (Not) required Linear model Proactive Explicit rectification) Safari DC cooling control Not required Thermodynamics DRL (reward shaping) [31] Required Neural network Reactive Semi-explicit Model-based* DC cooling control [16] Required Linear model – Implicit MPC [15] Building Required Gaussian process – Implicit *The “model” refers to that needed/used for learning the control policy toward the optimization objective, not for safety consideration. The three categories of model-free approaches are used as baselines for comparison when evaluating Safari in §6. † If the state transition is linear, [8, 11] do not require exploratory data. However, for nonlinear DC thermodynamics, although [8, 11] can be extended

## 公式/优化模型候选

### Page 1

```text
the energy efficiency of data center cooling. However, enforcement          In this paper, we consider the problem of DC cooling control
of thermal safety constraint during DRL’s state exploration is a         that aims at reducing the DC energy usage subject to the IT equip-
main challenge. The widely adopted reward shaping approach adds          ment’s thermal safety constraint. Any IT device specifies the highest
```
### Page 3

```text
on the single-hall scheme, which is often adopted in enterprise DCs.                 on the data hall’s cold aisle temperature (denoted by 𝑇in ). Therefore,
In §7, we will discuss how to extend Safari to address the multi-                    we model 𝑃IT = 𝑝 (𝑈 IT,𝑇in ). In the simulations conducted in this
hall scheme. The heat generated by the IT equipment is moved                         paper, we configure the EnergyPlus to use a model 𝑝 (𝑈 IT,𝑇in ) from
```
### Page 4

```text
cycles only. Appendix B provides a brief introduction of DX. Note                                                             𝜌𝑃IT [𝑘]
that Safari is agnostic to the type of cooling system. In §6, we will                                𝑇z [𝑘] = 𝑇ˆin [𝑘 − 1] +             .                    (2)
evaluate the performance of Safari for both CW and DX cooling.                                                               𝛼 𝑓ˆ[𝑘 − 1]
```
### Page 4

```text
governed by the following thermodynamic model derived from the                   uses the policy to select the action 𝜇 [𝑘] based on the current system
law of the conservation of energy [4]:                                           state 𝑠 [𝑘], i.e., 𝜇 [𝑘] = 𝜇𝜽 (𝑠 [𝑘]). The action drives the system to the
                                                                                 next state 𝑠 [𝑘 + 1], while the agent receives an immediate reward
```
### Page 4

```text
d𝑇z (𝑡)   𝑓 (𝑡)                       1                           𝑟 [𝑘]. Let 𝛾 denote a discounted factor. The agent uses an algorithm
                       =       (𝑇in (𝑡) − 𝑇z (𝑡)) +     𝑄 (𝑡),             (1)
                 d𝑡      𝜌𝑉s                        𝛼𝑉s                          to learn the optimal policy 𝜽 ∗ for the following                   unconstrained
```
### Page 4

```text
where 𝑡 is time, 𝑓 (𝑡) is the instantaneous total mass flow rate of                                            ∗
                                                                                 optimization problem: 𝜽 = arg max𝜽 E𝑠                     ∞   𝛾  𝑘 𝑟 [𝑘]  𝜇 𝜽   .
                                                                                                                                           𝑘=0
```
### Page 4

```text
workers temporarily in the data hall, and the external heat trans-
                                                                                 data samples {𝑠𝑖 , 𝜇𝑖 , 𝑠𝑖+1, 𝑟𝑖 |𝑖 = 1, . . . , 𝑁 } through interacting with
ferred into the data hall via walls. As the IT-generated heat usually
```
### Page 4

```text
dominates 𝑄 (𝑡), to simplify the discussion in this paper, we assume
                                                                                 function L (𝝓) = 𝑁1 𝑖=1        (𝑄 𝝓 (𝑠𝑖 , 𝜇𝑖 ) −𝑦𝑖 ) 2 , where 𝑦𝑖 is the target Q
                                                                                                          Í𝑁
```
### Page 4

```text
Í𝑁
𝑄 (𝑡) = 𝑃 IT (𝑡). Note that in the EnergyPlus simulations conducted
                                                                                 value given by 𝑦𝑖 = 𝑟𝑖 +𝛾𝑄 𝝓′ (𝑠𝑖+1, 𝜇𝜽′ (𝑠𝑖+1 )). The 𝑄 𝝓′ and 𝜇𝜽′ are two
```
### Page 4

```text
𝑄 (𝑡) = 𝑃 IT (𝑡). Note that in the EnergyPlus simulations conducted
                                                                                 value given by 𝑦𝑖 = 𝑟𝑖 +𝛾𝑄 𝝓′ (𝑠𝑖+1, 𝜇𝜽′ (𝑠𝑖+1 )). The 𝑄 𝝓′ and 𝜇𝜽′ are two
in this paper, we account for lighting heat. To achieve the uniform
```
### Page 5

```text
160          ∏T = 1                      160
                                                                                                     ∏T =                             ∏T = 1             ∏T = 0
```
### Page 5

```text
Baseline
 Temperature (±C)
```
### Page 5

```text
on the probability distributions of s[𝑘] and µ[𝑘] only. Thus, the
control process is an MDP.                                                                                                                                        (c)                              (d)
```
### Page 5

```text
and also includes a shaping term for thermal safety consideration:                                                                                            temperature.
 𝑟 [𝑘] =𝜆T exp −𝜆1 (𝑇z [𝑘] − 𝑇C ) 2 − 𝜆P 𝑃DC [𝑘] · · · · · · goals
                                   
```
### Page 5

```text
−𝜆2 (𝑇z [𝑘] − 𝑇U ) + (𝑇L − 𝑇z [𝑘]) + ,
                          +                          · · · shaping                                                                                            𝜆T when 𝜆P = 10−5 . We train a separate DDPG agent for each 𝜆T
                                                                                                                                                              setting. Each error bar shows the distribution of 𝑇z during testing.
```
### Page 5

```text
When 𝜆T ≠ 0, the 𝑇z fluctuates around 𝑇C and the variation of 𝑇z
the DC’s total power usage (i.e., 𝑃DC [𝑘] = 𝑃IT [𝑘] + 𝑃 c [𝑘]), [𝑇L,𝑇U ]
                                                                                                                                                              decreases with 𝜆T . When 𝜆T = 0, 𝑇z has large variations. Next, we
```
### Page 5

```text
the DC’s total power usage (i.e., 𝑃DC [𝑘] = 𝑃IT [𝑘] + 𝑃 c [𝑘]), [𝑇L,𝑇U ]
                                                                                                                                                              decreases with 𝜆T . When 𝜆T = 0, 𝑇z has large variations. Next, we
specifies a desirable range for 𝑇z [𝑘], (𝑥) + = max{0, 𝑥 }. The shap-
```
### Page 5

```text
decreases with 𝜆T . When 𝜆T = 0, 𝑇z has large variations. Next, we
specifies a desirable range for 𝑇z [𝑘], (𝑥) + = max{0, 𝑥 }. The shap-
                                                                                                                                                              fix 𝜆T to a certain setting and vary 𝜆P . For each 𝜆P , we train multiple
```
### Page 6

```text
Fig. 4: DDPG’s training phase under various temperature set-                                                     hÍ               i
points. (a) Cumulative count of safety violations; (b) viola-                                                      ∞ 𝛾 𝑘 𝑟 [𝑘] µ ,
                                                                                          𝜽 ∗ = arg max𝜽 E𝑃IT ,𝑇o 𝑘=0           𝜽
```
### Page 6

```text
points. (a) Cumulative count of safety violations; (b) viola-                                                      ∞ 𝛾 𝑘 𝑟 [𝑘] µ ,
                                                                                          𝜽 ∗ = arg max𝜽 E𝑃IT ,𝑇o 𝑘=0           𝜽
tion magnitude: mid line, box, and whisker represent me-
```
### Page 6

```text
                                                   (4)
                                                                                          s.t. Pr 𝑇z [𝑘] ≤ 𝑇 z > 1 − 𝜖, ∀𝑘,
dian, interquartile range, and degree of dispersion.
```
### Page 6

```text
The above results show that, under a certain 𝑇C setting, the
                                                                           the constraint in Eq. (4), after an action µ[𝑘] is recommended by the
DDPG agent achieves substantial power savings compared with
```
### Page 6

```text
DDPG agent achieves substantial power savings compared with
                                                                           DDPG agent at 𝑡 = 𝑘𝜏, we use the state transition model obtained
the baseline controller. In addition, under the conventional control
```
### Page 6

```text
that maintains 𝑇z at 𝑇C , running hotter data center (i.e., by setting
                                                                           µ[𝑘] at the end of the control period. Denote by 𝑇ez [𝑘 + 1] the
higher 𝑇C ) can be beneficial to energy efficiency [13], due primarily
```
### Page 6

```text
higher 𝑇C ) can be beneficial to energy efficiency [13], due primarily
                                                                           predicted temperature and by 𝑇ez [𝑘 + 1] = ℎ(µ[𝑘], 𝑃IT [𝑘], · · · ) the
to the saving in cooling power. However, from Fig. 3(c), under
```
### Page 6

```text
proposed DDPG agent jointly considers the impacts of 𝑇ˆin and 𝑓ˆ
                                                                           𝑇 z , we solve the following problem to find a rectified action µ∗ [𝑘]:
on the IT/cooling power and minimizes the DC total power.
```
### Page 7

```text
Limit (𝜽 ) = 𝑀1 𝑚=0    ∥µ𝜽 (ssafe [𝑚]) − asafe [𝑚] ∥ 22 . On the comple-
                 Í𝑀
```
### Page 7

```text
As discussed in §5.1, the accuracy of the state transition model                     The LSTMs trained with non-exploratory and marginally safe ex-
ℎ(µ[𝑘], 𝑃IT [𝑘], · · · ) is critical to the safety compliance of post-hoc            ploratory data have high MAEs, due to their poor performance in
rectification. In this section, we first discuss a possible design that              characterizing the transitions to unsafe states. Fig. 7 includes results
```
### Page 7

```text
the transient model in Eq. (1) and is unleashed from Assumption 2.                   controller to fit the parameter 𝛼 in Eq. (2). Then, Safari-1 uses Eq. (2)
Based on Safari-2, Safari-3 applies the maximum ramp-up trajectory                   as the prediction model 𝑇ez [𝑘 + 1] = ℎ(µ[𝑘], 𝑃IT [𝑘], · · · ). If 𝑇ez [𝑘 + 1]
of 𝑃IT observed in history as the predicted trajectory within the
```
### Page 7

```text
5.3.1 A possible design of LSTM-based rectification. LSTM net-                                          
                                                                                                        𝑇ˆin∗ [𝑘] − 𝑇ˆin [𝑘] + 𝜆 = 0,
works can model complex and nonlinear temporal correlations.                                            
```
### Page 7

```text
However, the non-exploratory data generated by the conventional                                         
                                                                                                                                                = 0,
                                                                                                                                 𝛼 ( 𝑓ˆ∗ [𝑘]) 2
```
### Page 7

```text
ˆ∗         𝑃IT [𝑘 + 1]
                                                                                                        𝜆 𝑇in [𝑘] +                     − 𝑇 z = 0,
                                                                                                        
```
### Page 7

```text

past 20 control periods. We conduct experiments to investigate the                   where 𝜆 is the Lagrangian multiplier, µ∗ [𝑘] = (𝑇ˆin∗ [𝑘], 𝑓ˆ∗ [𝑘]) is the
LSTM’s requirement on training data. Fig. 6 shows the distribu-                      rectified action. Under the definition 𝑃IT [𝑘+1] ≜ 𝑃IT (𝑡)|𝑡 ∈(𝑘𝜏,(𝑘+1)𝜏) ,
```
### Page 8

```text
𝑡 = 𝑘𝜏. However, pragmatically, the controller can wait for a short
while until 𝑃IT [𝑘 + 1] is observable and then solve Eq. (6).
```
### Page 8

```text
𝑄 (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) remains constant at 𝑃 IT [𝑘 + 1]. For 𝑇in (𝑡) and         uses the design of [11] to predict the next state temperature by
𝑓 (𝑡), we adopt their setpoints as their approximations. Specifically,         𝑇ez [𝑘 + 1] = 𝑔𝜔 (s[𝑘]) ⊺ µ[𝑘] + 𝑇z [𝑘] where 𝑔𝜔 is modeled using a
we set 𝑇in (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) = 𝑇ˆin [𝑘] and 𝑓 (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) = 𝑓ˆ[𝑘].    three-layer MLP to predict the linear correlation coefficients and
```
### Page 8

```text
𝑓 (𝑡), we adopt their setpoints as their approximations. Specifically,         𝑇ez [𝑘 + 1] = 𝑔𝜔 (s[𝑘]) ⊺ µ[𝑘] + 𝑇z [𝑘] where 𝑔𝜔 is modeled using a
we set 𝑇in (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) = 𝑇ˆin [𝑘] and 𝑓 (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) = 𝑓ˆ[𝑘].    three-layer MLP to predict the linear correlation coefficients and
Then, with the initial condition 𝑇z (𝑘𝜏) = 𝑇z [𝑘], we can solve 𝑇z (𝑡)          each layer has 32 neurons. If only non-exploratory training data
```
### Page 8

```text
we set 𝑇in (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) = 𝑇ˆin [𝑘] and 𝑓 (𝑡)|𝑡 ∈ [𝑘𝜏,(𝑘+1)𝜏) = 𝑓ˆ[𝑘].    three-layer MLP to predict the linear correlation coefficients and
Then, with the initial condition 𝑇z (𝑘𝜏) = 𝑇z [𝑘], we can solve 𝑇z (𝑡)          each layer has 32 neurons. If only non-exploratory training data
                                                               ˆ
```
### Page 8

```text
ˆ
from Eq. (1) as 𝑇z (𝑡) = 𝑊 [𝑘] + (𝑇z [𝑘] − 𝑊 [𝑘]) e− 𝑓 [𝑘 ] (𝑡 −𝑘𝜏)/𝑉s ,        produced by the baseline controller are used, Safari-2 achieves
                                                  𝑃 [𝑘+1]
```
### Page 8

```text
𝑃 [𝑘+1]
𝑡 ∈ [𝑘𝜏, (𝑘 + 1)𝜏), where 𝑊 [𝑘] = 𝑇ˆin [𝑘] + IT ˆ             is a constant     the lowest MAEs of less than 0.9°C. In addition, the steady-state
                                                   𝛼 𝑓 [𝑘 ]
```
### Page 8

```text
making approximations for 𝑇in (𝑡) and 𝑓 (𝑡) by adopting the average            linear prediction model in [11]. The performance of the various
                                                    ∫ (𝑘+1)𝜏                    designs of Safari in preventing safety violations will be extensively
 of 𝑇z (𝑡) as the prediction, i.e., 𝑇ez [𝑘 + 1] = 𝜏1 𝑘𝜏      𝑇z (𝑡)d𝑡.          evaluated in §6.
```
### Page 8

```text
∫ (𝑘+1)𝜏                    designs of Safari in preventing safety violations will be extensively
 of 𝑇z (𝑡) as the prediction, i.e., 𝑇ez [𝑘 + 1] = 𝜏1 𝑘𝜏      𝑇z (𝑡)d𝑡.          evaluated in §6.
     Safari-2 uses the above heuristic prediction approach to predict
```
### Page 11

```text
𝑃c                    cooling power usage
    𝑃 DC                  DC total power usage, 𝑃DC = 𝑃IT + 𝑃c
    𝑈 IT                  IT utilization
```
### Page 11

```text
𝑈 IT                  IT utilization
    𝑄                     sensible heat load, 𝑄 = 𝑃IT in analysis
                                                                                                             DX evaporator
```
### Page 11

```text
𝜏                     control period                                             C      USED SETTINGS AND DATA
    µ                     action, µ = (𝑇ˆin, 𝑓ˆ)                                     Table 4 summarizes the hyperparameter settings of DDPG. Figs. 12(a)
    s                     state s = (𝑇z,𝑇in, 𝑃c, 𝑃IT,𝑇o )                            and (b) show the outdoor air temperature and IT utilization data
```
### Page 11

```text
µ                     action, µ = (𝑇ˆin, 𝑓ˆ)                                     Table 4 summarizes the hyperparameter settings of DDPG. Figs. 12(a)
    s                     state s = (𝑇z,𝑇in, 𝑃c, 𝑃IT,𝑇o )                            and (b) show the outdoor air temperature and IT utilization data
    𝑟                     reward function                                            used for evaluation. The outdoor temperature data, which are pro-
```
### Page 11

```text
Actor hidden layer       [32, 32]   Critic hidden layer      [32, 32]           servers [1].
     replay buffer size       1 × 107    Discounted factor (𝛾 )   0.99
```

## 符号表/变量定义候选

未发现明确 Nomenclature/Acronyms 段。

## 面向本项目的复现接口

- 输入侧优先映射：天气/湿球温度、IT cooling load、TOU 电价、PV 预测、TES/SOC 初始状态、设备容量和效率曲线。
- 状态侧优先映射：TES/SOC、室温或供回水温度、设备启停状态、储冷/释冷功率。
- 决策侧优先映射：chiller/heat-exchanger/pump/fan 负荷率、储冷/释冷动作、MPC 滚动时域控制量。
- 目标侧优先映射：能耗、电费、PUE、约束违背惩罚、启停或动作平滑惩罚。
- 验证边界：任何文献节能率或控制效果都不能直接迁移为本项目结果，必须通过本地模型或 `mpc_v2` 验证矩阵复核。
