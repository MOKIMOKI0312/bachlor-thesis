# 核心公式、伪代码和算法

## 抽取边界

- 来源 PDF：`2017_Li_Transforming_Cooling_Optimization_for_Green_Data_Center_via_Deep_Reinfor.pdf`
- 标题：Transforming Cooling Optimization for Green Data Center via Deep Reinforcement Learning
- 抽取重点：公式、优化目标、约束、MPC/MILP/控制流程、输入输出变量。
- 注意：PDF 公式转文本可能丢失上下标、希腊字母、分式结构和跨栏顺序；这里是低 token 检索索引，不是最终论文公式稿。

## 人工核对页线索

自动抽取；未做人工页码核对。

无人工补充说明。

## 算法/控制流程候选

### Page 1

Transforming Cooling Optimization for Green Data Center via Deep Reinforcement Learning Yuanlong Li, Yonggang Wen, Kyle Guan, and Dacheng Tao
### Page 1

Abstract—Cooling system plays a critical role in a modern sources of energy consumption in DC (about 38% [3]), the data center (DC). Developing an optimal control policy for DC cooling energy. cooling system is a challenging task. The prevailing approaches Cooling energy optimization involves the control of a so- often rely on approximating system models that are built upon the knowledge of mechanical cooling, electrical and thermal phisticated cooling system, which consists of multiple compo- arXiv:1709.05077v4 [cs.AI] 18 Jul 2018
### Page 3

combined with IT side optimization to reduce the energy cost of DC further. We note that most of the existing studies are based on ideal models in a simplified situation. In this paper, we propose a new DRL based solution and verify the approach on both complicated simulation system and a real data trace.
### Page 5

a single offline trace for batch learning. In the following, we introduce the details of the proposed algorithm.
### Page 7

TABLE I C OMPARISON OF THE PROPOSED CCA WITH THE BASELINE ALGORITHMS : D EFAULT E+ AND TS. T HE RESULTS ARE BASED ON 10 TIMES OF INDEPENDENT TESTS .
### Page 7

reasonable as in the real case designing a special optimization algorithm is not an easy task. For the proposed CCA and the TS optimization algorithms, the optimal control settings generated by these two algorithms are tested by simulation on the EnergyPlus platform. That is, Fig. 4. Distribution of PUE and maximum outlet temperature Tz1 and Tz2 over the test period achieved by different control algorithms, respectively. The for each state at the testing phase, we use the settings provided results are based on 10 independent tests. The orange line in each box is the by the CCA or the TS algorithm and then record the resulting median, and the green triangle is the mean value. state changes and rewards for performance evaluation.
### Page 7

PUE In this section, we present the simulation results of the 1.3 CCA average PUE and maximum outlet temperature (during the test period), obtained by using DefaultE+, TS, and CCA 1.2 0 50000 100000 150000 200000 250000 algorithms. Based on these results we further compare and Time (unit: 6mins) evaluate the underlying control algorithms. Table I shows the first and second-order statistics of the (a) PUE and maximum outlet temperature in 10 independent runs with different τ settings for TS and CCA. For better examining Temperature
### Page 7

DefaultE+ Tz1 these results, we also plot the data distribution in Fig. 4. We 30.0 TS Tz1 can observe the following: CCA Tz1 27.5 • The proposed CCA algorithm with τ = 1 achieves the best control results, by reducing the PUE from 1.37 to 0 50000 100000 150000 200000 250000 1.33, which is 11% cooling power saving, while main- Time (unit: 6mins)
### Page 7

control settings. DefaultE+ Tz2 30.0 • The TS algorithm with the general optimization algorithm TS Tz2 shows unstable performance. To improve its performance, 27.5 CCA Tz2
### Page 9

26.75 Real TABLE III Predicted without DUE 26.50 T HE DATA ENTRIES AND THEIR USAGE IN OPTIMIZATION OF THE AIRFLOW Predicted with DUE Temperature (C)

## 公式/优化模型候选

### Page 3

```text
Luo et al. proposed a neural actor-critic RL solution to the
H∞ control problem; in [33], Liu et al. proposed a single
critic network based RL solution to the constrained-input
```
### Page 3

```text
the robustness requirement is high. In this paper, we propose      other sources (such as illuminations), with ITE as the dominant
to adopt the DDPG algorithm for the cooling system control         heat source. The load of the ITE is defined as α · L, where L
optimization problem and examine various implementation            is the designed load density (per square meter) and α is a load
```
### Page 3

```text
to adopt the DDPG algorithm for the cooling system control         heat source. The load of the ITE is defined as α · L, where L
optimization problem and examine various implementation            is the designed load density (per square meter) and α is a load
details related to the robustness of the algorithm.                factor that varies at different time slots. In our simulation, we
```
### Page 3

```text
details related to the robustness of the algorithm.                factor that varies at different time slots. In our simulation, we
                                                                   use a public trace collected from Wikimedia [42] to set α to be
       III. T HE C OOLING O PTIMIZATION P ROBLEM                   the same for z1 and z2 while using different load density 4kw
```
### Page 4

```text
overheating (for both z1 and z2 ). The penalty function has         Off-policy algorithms generally employ a separate behavior
the form of λ ·ln(1 + exp(Tzi − φ)), for i ∈ [1, 2], with           policy, which is independent of the policy being estimated,
λ, Tzi , and φ denoting the penalty pricing factor, average         to generate the training trace; while on-policy directly uses
```
### Page 4

```text
the form of λ ·ln(1 + exp(Tzi − φ)), for i ∈ [1, 2], with           policy, which is independent of the policy being estimated,
λ, Tzi , and φ denoting the penalty pricing factor, average         to generate the training trace; while on-policy directly uses
IT equipment outlet temperature of zone i, and overheating          control policy being estimated (in the real control practice or
```
### Page 5

```text
min                 pue + λ · ln(1 + exp(Tz1 − φ)) + λ · ln(1 + exp(Tz2 − φ)),                                       (1)
                       Tcw ,Tdx ,Tdec ,Tiec ,Tch
```
### Page 5

```text
Tcw ,Tdx ,Tdec ,Tiec ,Tch
                                            s.t.    LTcw ≤ Tcw ≤ UTcw ,
                                                    LTdx ≤ Tdx ≤ UTdx ,
```
### Page 5

```text
s.t.    LTcw ≤ Tcw ≤ UTcw ,
                                                    LTdx ≤ Tdx ≤ UTdx ,
                                                    LTdec ≤ Tdec ≤ UTdec ,
```
### Page 5

```text
LTdx ≤ Tdx ≤ UTdx ,
                                                    LTdec ≤ Tdec ≤ UTdec ,
                                                    LTiec ≤ Tiec ≤ UTiec ,
```
### Page 5

```text
LTdec ≤ Tdec ≤ UTdec ,
                                                    LTiec ≤ Tiec ≤ UTiec ,
                                                    LTch ≤ Tch ≤ UTch .
```
### Page 5

```text
LTiec ≤ Tiec ≤ UTiec ,
                                                    LTch ≤ Tch ≤ UTch .
```
### Page 5

```text
PUE and temperature data. Different to canonical RL approach
in which the future reward data are also included in the                   Fig. 3. Architecture of the Q and µ network. The design follows the standard
                                                                           actor-critic architecture. One thing notable is that the second last layer of Q
```
### Page 5

```text
and validation data.                                                       output of Q when taking action at current state according to
   Before the training starts, we first initialize two neural net-         the output of µ. To avoid over-fitting, we also compute the
works (line 2). The critic network Q(XQ |θQ ) (parameterized               validation error to keep track of the best weight parameter
```
### Page 6

```text
data series of the state, action and reward data. Organize    the same data for the proposed approach.
    the data into the form of XQ , Xµ , and yr , where XQ
    is the input of the Q-neural network Q, Xµ is the input
```
### Page 6

```text
the data into the form of XQ , Xµ , and yr , where XQ
    is the input of the Q-neural network Q, Xµ is the input
                                                                  A. Simulation Configurations
```
### Page 6

```text
A. Simulation Configurations
    of the policy network µ, and yr is the energy(PUE) and
    temperature readings that can be used to compute the loss        We use EnergyPlus to collect the training data and assess
```
### Page 6

```text
data in the validation set.                                   partment of Energy Building Technologies Office, is a widely
 2: Initialization: Create neural networks Q(XQ |θ Q ) and        recognized and reliable simulation platform to model building
    µ(Xµ |θµ ), and randomly set the values of the weight         cooling energy consumption. Third, EnergyPlus provides the
```
### Page 6

```text
2: Initialization: Create neural networks Q(XQ |θ Q ) and        recognized and reliable simulation platform to model building
    µ(Xµ |θµ ), and randomly set the values of the weight         cooling energy consumption. Third, EnergyPlus provides the
                                                           Q
```
### Page 6

```text
Q
    parameters θQ and θµ . Set initial validation error Eval =    flexibility that allows simulation with user-defined algorithms,
                 µ
```
### Page 6

```text
µ
    1e100, Eval = 1e100 and Qbest = N ull, µbest = N ull.         control actions, and schedules.
 3: for Epoch = 1, ..., M axEpoch do                                 The simulation is configured as follows. We adopt the
```
### Page 6

```text
1e100, Eval = 1e100 and Qbest = N ull, µbest = N ull.         control actions, and schedules.
 3: for Epoch = 1, ..., M axEpoch do                                 The simulation is configured as follows. We adopt the
 4:     Randomly divide the training data into batches of size    original DC model provided by the EnergyPlus platform
```
### Page 7

```text
DefaultE+       TS τ = 1     CCA τ = 1       TS τ = 3      CCA τ = 3
   PUE       1.376±0.000     1.371±0.000    1.333±0.003   1.346±0.023    1.307±0.017
```
### Page 7

```text
DefaultE+       TS τ = 1     CCA τ = 1       TS τ = 3      CCA τ = 3
   PUE       1.376±0.000     1.371±0.000    1.333±0.003   1.346±0.023    1.307±0.017
 max(Tz1 )   28.617±0.000   26.242±0.000   28.910±0.361   30.995±3.121   30.111±2.759
```
### Page 7

```text
PUE       1.376±0.000     1.371±0.000    1.333±0.003   1.346±0.023    1.307±0.017
 max(Tz1 )   28.617±0.000   26.242±0.000   28.910±0.361   30.995±3.121   30.111±2.759
 max(Tz2 )   28.655±0.000   26.288±0.000   29.124±0.353   30.291±3.307   33.250±0.005
```
### Page 7

```text
max(Tz1 )   28.617±0.000   26.242±0.000   28.910±0.361   30.995±3.121   30.111±2.759
 max(Tz2 )   28.655±0.000   26.288±0.000   29.124±0.353   30.291±3.307   33.250±0.005
```
### Page 7

```text
PUE and maximum outlet temperature in 10 independent runs
with different τ settings for TS and CCA. For better examining
                                                                                           Temperature
```
### Page 7

```text
27.5
   • The proposed CCA algorithm with τ = 1 achieves the
      best control results, by reducing the PUE from 1.37 to                                                     0       50000   100000     150000      200000      250000
```
### Page 7

```text
accuracy problem in real time.
   • The recurrent version of CCA with τ = 3 has unstable
                                                                                        beginning of the test period, the temperature of the DC zone
```
### Page 8

```text
CCA                 TargetNet             ReluNet               LstmNet                                                                                30
  PUE       1.333e+00±3.371e-03   1.338e+00±1.301e-03   1.326e+00±2.128e-07   1.343e+00±2.305e-02
max(Tz1 )   2.891e+01±3.614e-01   2.850e+01±1.281e-01   2.949e+01±7.958e-14   2.874e+01±2.186e+00            1.32
```
### Page 8

```text
PUE       1.333e+00±3.371e-03   1.338e+00±1.301e-03   1.326e+00±2.128e-07   1.343e+00±2.305e-02
max(Tz1 )   2.891e+01±3.614e-01   2.850e+01±1.281e-01   2.949e+01±7.958e-14   2.874e+01±2.186e+00            1.32
max(Tz2 )   2.912e+01±3.525e-01   2.867e+01±1.208e-01   2.951e+01±1.709e-09   2.864e+01±2.640e+00
```
### Page 8

```text
max(Tz1 )   2.891e+01±3.614e-01   2.850e+01±1.281e-01   2.949e+01±7.958e-14   2.874e+01±2.186e+00            1.32
max(Tz2 )   2.912e+01±3.525e-01   2.867e+01±1.208e-01   2.951e+01±1.709e-09   2.864e+01±2.640e+00
                                                                                                                                                                           28
```
### Page 8

```text
not; 3) LstmNet: with a LSTM layer to process the recent
history trace. As we use the recent states and actions in the                                       Fig. 6. Testing different settings for λ from 0.0 to 0.04.
last τ steps as the input to the Q network, we can use an
```
### Page 8

```text
history trace. As we use the recent states and actions in the                                       Fig. 6. Testing different settings for λ from 0.0 to 0.04.
last τ steps as the input to the Q network, we can use an
LSTM layer to process it first as LSTM is a recurrent neural
```
### Page 8

```text
normal Q network as described in Section IV-D. We test these
different designs with τ set to 1; for LstmNet, we show the
results with τ set to 3.
```
### Page 8

```text
different designs with τ set to 1; for LstmNet, we show the
results with τ set to 3.
   Test results of these different designs are shown in Table                                                                                              0DQDJHPHQW
```
### Page 8

```text
layers and LSTM layers can slow down the training speed, we                                         to problem. For example, in the next section, we will show
adopt the simpler design illustrated in Fig. 3.                                                     the result of a different test case, in which the best λ setting
                                                                                                    is different from this simulation case.
```
### Page 9

```text
Fig. 8. Testing different τ settings from 1 to 10 for the NSCC data trace.
Results show that with τ =4, the best Q learning quality can be achieved with     Fig. 10. Testing different λ settings for the NSCC case. Energy saving
```
### Page 9

```text
Results show that with τ =4, the best Q learning quality can be achieved with     Fig. 10. Testing different λ settings for the NSCC case. Energy saving
the lowest MAE around 0.094 for the temperature prediction.                       decreases when λ increases, at the same time the maximum temperature
                                                                                  decreases.
```
### Page 9

```text
X
                                                                                                            Eval ←              max(y(j) − Q(XQ (j)|θQ , 0).                                (1)
proposed algorithm to train the Q and µ network. In this case,
```
### Page 9

```text
Eval ←              max(y(j) − Q(XQ (j)|θQ , 0).                                (1)
proposed algorithm to train the Q and µ network. In this case,
                                                                                                                         j∈IV
```
### Page 9

```text
Experimental results demonstrate that the proposed algo-                          Second, we study how much energy saving we can achieve
rithm works well as expected. First, the results show that the                    when using different penalty parameter λ (used in the objective
normalized mean absolute error (MAE) of the Q network is                          function (1)), as shown in Fig. 10. In Fig. 11 we also show
```
### Page 9

```text
smaller than 0.1 degrees, as shown in Fig. 8. This proves that                    how the control actions and predicted temperature change
the network can successfully capture the system dynamics.                         with different λ. From Fig. 10 we can see that with DUE,
Fig. 8 also shows that when dealing with real data with noise,                    we can achieve energy saving 15% easily if we set the
```
### Page 9

```text
recurrent decision making is better as we found that when set-                    temperature threshold larger than 26.6 degrees. If we want
ting τ = 4 we can get the best temperature estimation results.                    a lower maximum temperature such as 26.4, we can still save
Even though MAE lower than 0.1 degree seems great, it can                         about 10%. When we set the target maximum temperature
```
### Page 10

```text
Energy and buildings, vol. 34, no. 1, pp. 103–109, 2002.
Fig. 11. Test results on the NSCC data trace when λ changed from 18 to                 [10] R. S. Sutton and A. G. Barto, Reinforcement learning: An introduction.
26: (a) control setting (airflow rate) predicted by the proposed algorithm; (b)             MIT press Cambridge, 1998, vol. 1, no. 1.
```
### Page 10

```text
the correspondingly predicted rack temperature. Darker color curves are with           [11] A. G. Barto, R. S. Sutton, and C. W. Anderson, “Neuronlike adaptive
smaller λ settings, and the black curve (with ”+” marker) is the real data trace            elements that can solve difficult learning control problems,” IEEE
collected in the test period.                                                               transactions on systems, man, and cybernetics, no. 5, pp. 834–846, 1983.
```
### Page 11

```text
robust controller design for continuous-time uncertain nonlinear systems         http://pemami4911.github.io/blog/2016/08/21/ddpg-rl.html,        accessed:
     subject to input constraints,” IEEE transactions on cybernetics, vol. 45,   2017-03-01.
```

## 符号表/变量定义候选

未发现明确 Nomenclature/Acronyms 段。

## 面向本项目的复现接口

- 输入侧优先映射：天气/湿球温度、IT cooling load、TOU 电价、PV 预测、TES/SOC 初始状态、设备容量和效率曲线。
- 状态侧优先映射：TES/SOC、室温或供回水温度、设备启停状态、储冷/释冷功率。
- 决策侧优先映射：chiller/heat-exchanger/pump/fan 负荷率、储冷/释冷动作、MPC 滚动时域控制量。
- 目标侧优先映射：能耗、电费、PUE、约束违背惩罚、启停或动作平滑惩罚。
- 验证边界：任何文献节能率或控制效果都不能直接迁移为本项目结果，必须通过本地模型或 `mpc_v2` 验证矩阵复核。
