description: Minimal rebuilt MPC v1 validation matrix.
steps: 96
horizon_steps: 48
output_root: runs/mpc_v2_matrix
scenarios:
  - case_id: rebuild_no_tes_24h
    controller_mode: no_tes
  - case_id: rebuild_rbc_24h
    controller_mode: rbc
  - case_id: rebuild_mpc_24h
    controller_mode: mpc
    initial_soc: 0.5
    soc_target: 0.5
    truncate_horizon_to_episode: true
