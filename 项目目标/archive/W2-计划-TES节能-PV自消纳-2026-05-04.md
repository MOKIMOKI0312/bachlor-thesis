# W2 计划：TES 节能 + PV 自消纳全年对照

**日期**：2026-05-04
**主线**：W1（forecast noise 鲁棒性 13-cell batch）已收官 → W2 切到「TES 节能 + PV 自消纳全年对照」
**关联**：`项目目标/技术路线.md` §6 / `项目目标/3周收尾路线-MILP-MPC-2026-05-03.md` "2026-05-04 W2 主线修订" 段
**状态**：plan 已审定（ExitPlanMode 通过）；本文件是 Codex 自动化执行版

---

## Context

W1 已产出 13-cell MILP forecast-noise robustness 数据。但这一组数据只解决了 **"MPC 内部稳不稳"** 问题，没有讲清楚 **"为什么要装 TES、为什么要用 MPC"** 的工程价值。

**W2 主线**：3 算法（MPC-MILP / MPC-Heuristic / no-TES baseline）× 全年 trainlike（35040 step）对照，产出论文 §4.1 节能 + §4.2 PV 自消纳两章数据基础。

**论文 §4 新结构**：
- §4.1 TES 节能贡献：3 算法 cost / PUE / comfort 全年对比 + 论文表中追加 "Xiao&You-SAC reported, PUE~1.40" 行作学术对标。
- §4.2 TES 对 PV 自消纳的提升：3 算法 self-consumption rate 对照 + 机理说明（江苏 TOU **尖峰 19–21h（190 USD/MWh）PV=0**，PV 峰值 10–16h 落在 **TOU 平段（83 USD/MWh）**，TES 白天充冷消纳 PV → 晚上尖峰放冷既提升 PV 自消纳又抓 TOU 套利）。
- 附录 A：W1-3 robustness curve（forecast noise 衰减作为方法 generalization 证据）。

**Baseline 复现策略**：不做 SAC 复现。代码侧 baseline 用 `tools/evaluate_m2_rule_baseline.py --policy neutral`（真正的 no-TES：valve 永远=0）。论文 §4.1 表中以「Xiao&You-SAC reported, PUE~1.40」行直接引用 Xiao & You 2026 论文报告值。

---

## 不变量

**新增 / 改：**
- `tools/run_w2_scenario_batch.ps1`（新增，PowerShell 5.1 兼容）
- `tools/build_w2_scenario_summary.py`（新增；含 PV self-consumption 计算）
- `analysis/m2f1_w2_scenario_compare_${TS}.csv` + `.md`（产出）

**不改：**
- `tools/m2_tes_mpc_oracle.py`（W1 已稳）
- `tools/evaluate_m2_rule_baseline.py`（已存在，是 trainlike + 4D action + 完整 wrapper 链 + `--policy {neutral, charge, discharge, tou}` 的 rule-based 评估工具）
- `tools/evaluate_baseline.py`（**不用**——它是 evaluation_flag=1 + ITE_Set=1.0 + timesteps_per_hour=1 的 8760 step 工具，与 MPC 不可直接对比）
- 任何 epJSON / EMS Program / wrapper / reward
- W1 已落盘的 27 个 oracle runs

---

## 实验矩阵（3 cells × 全年 trainlike）

| ID | tag | 工具 | epJSON | 算法 | step | 估时 |
|---|---|---|---|---|---|---|
| 1 | `w2_mpc_milp_year_${TS}` | `m2_tes_mpc_oracle.py --solver milp` | DRL_DC_training | MILP 24h horizon | 35040 | ~60–90 min |
| 2 | `w2_mpc_heuristic_year_${TS}` | `m2_tes_mpc_oracle.py --solver heuristic` | DRL_DC_training | rule-based with TES | 35040 | ~3–5 min |
| 3 | `w2_baseline_neutral_year_${TS}` | `evaluate_m2_rule_baseline.py --policy neutral` | DRL_DC_training | **no-TES**（valve 永远=0）| 35040 | ~3–5 min |

**关键设计决策**：3 cells 全部 trainlike + 35040 step + ITE_Set=0.45 + 同 wrapper 链（PVSignalWrapper / PriceSignalWrapper / TESTargetValveWrapper）→ schema 严格一致，可以直接对比。

`${TS}` 三 cell 共用同一时间戳（`yyyyMMdd_HHmmss`），便于聚合。

总 wall-clock：~70–100 min（MILP 主导）。

---

## PV 自消纳率计算（论文 §4.2 核心新指标）

**已 verify monitor.csv 字段**：
- `current_pv_kw`（PV 实际发电 kW，由 PVSignalWrapper 注入）
- `Electricity:Facility`（建筑总用电 J/step）
- `ITE-CPU:InteriorEquipment:Electricity`（IT 设备 J/step，仅参考）

**计算公式（聚合脚本里实现，dt = 0.25 h）**：

```python
pv_kw   = df["current_pv_kw"].astype(float).to_numpy()
fac_J   = df["Electricity:Facility"].astype(float).to_numpy()
step_seconds = 3600.0 / 4  # timesteps_per_hour=4
load_kw = fac_J / step_seconds / 1000.0  # J/step → kW

pv_consumed_kw = np.minimum(pv_kw, load_kw)
dt_h = 0.25  # 15 min step

pv_total_gen_mwh    = pv_kw.sum() * dt_h / 1000.0
pv_consumed_mwh     = pv_consumed_kw.sum() * dt_h / 1000.0
self_consumption_rate_pct = pv_consumed_mwh / pv_total_gen_mwh * 100.0
pv_load_coverage_pct      = pv_consumed_mwh / (load_kw.sum() * dt_h / 1000.0) * 100.0
grid_import_mwh           = np.maximum(0, load_kw - pv_kw).sum() * dt_h / 1000.0
grid_export_mwh           = np.maximum(0, pv_kw - load_kw).sum() * dt_h / 1000.0
```

**物理上限**：PV 6 MWp / 负载峰值 6 MWave ≈ 1:1，PV 仅白天有发电，理论 SCR 上限 ~67%。

**预期对照（写入论文叙事）**：
- baseline_neutral：PV 白天发电直接被建筑实时消纳，SCR 由负载基线决定
- MPC：白天主动多充冷 → 把白天 PV 多消化掉 → SCR **应高于 baseline**
- Heuristic：因为不读 PV 信号（仅看 TOU 价格），与 MPC 接近 baseline

---

## Codex 自动化执行 playbook（PHASE 1–4）

### 顶层规则

- **不来回问**。schema 偏差 / 字段名变种 → 用预设 fallback 自己继续，不停下让用户决定。
- **只在以下情况停**：(a) Python 异常导致 cell 跑不了；(b) git commit/push 冲突；(c) 显然违反单调性的物理结果（cost_baseline < cost_milp）。
- 停的时候贴：完整 traceback / 失败 cell tag / monitor 行数 / result.json 头 30 行。
- 不修 epJSON / wrapper / reward / m2_tes_mpc_oracle.py / evaluate_m2_rule_baseline.py 核心逻辑。
- 不开 PR。直接 commit + push master。

---

### PHASE 1 — dry-run (96 step) + schema 探测

**P1a. 三 cell 96-step dry-run**：

```powershell
$TS_DRY = Get-Date -Format "yyyyMMdd_HHmmss"

# Cell 1: MPC-MILP dry-run
& D:/Anaconda/python.exe tools/m2_tes_mpc_oracle.py `
    --tag dryrun_w2_milp_96_$TS_DRY --eval-design trainlike --max-steps 96 `
    --out-dir runs/m2_tes_mpc_oracle --solver milp --forecast-noise-mode perfect

# Cell 2: MPC-Heuristic dry-run
& D:/Anaconda/python.exe tools/m2_tes_mpc_oracle.py `
    --tag dryrun_w2_heur_96_$TS_DRY --eval-design trainlike --max-steps 96 `
    --out-dir runs/m2_tes_mpc_oracle --solver heuristic --forecast-noise-mode perfect

# Cell 3: no-TES baseline dry-run
# evaluate_m2_rule_baseline.py 是否支持 --max-steps：先 read tools/evaluate_m2_rule_baseline.py
# 全文确认（≤350 行）。如果支持就传 96；如果不支持就跑完整 episode（35040 step ~5 min，
# 作为 dry-run 也接受）。两种情况都不停下。
& D:/Anaconda/python.exe tools/evaluate_m2_rule_baseline.py `
    --tag dryrun_w2_neutral_96_$TS_DRY --eval-design trainlike `
    --policy neutral [可能加 --max-steps 96]
```

**P1b. Schema 探测**（Python 直接读 monitor 列名 + result.json keys）：

使用 Python 一次性读三个 cell 的 monitor.csv columns 和 result.json keys，**打印输出但不要等用户确认**，按以下候选名走 fallback：

```python
PV_ALIASES = ["current_pv_kw", "pv_current_kw", "pv_kw", "pv_power_kw"]
FACILITY_ALIASES = ["Electricity:Facility", "Facility:Electricity", "Electricity_Facility"]
IT_ALIASES = ["ITE-CPU:InteriorEquipment:Electricity", "ITE_CPU_InteriorEquipment_Electricity"]
SOC_ALIASES = ["TES_SOC", "tes_soc"]
MODE_ALIASES = ["tes_mpc_mode_label"]   # 仅 MPC cell 有，baseline 没有 OK

COST_KEYS = ["cost_usd", "cost_usd_total", "total_cost_usd", "cost_total"]
PUE_KEYS = ["pue", "pue_avg", "PUE"]
COMFORT_KEYS = ["comfort_violation_pct", "comfort_pct", "comfort"]
SIGN_RATE_KEYS = ["charge_window_sign_rate"]
DSOC_PREPEAK_KEYS = ["delta_soc_prepeak", "dsoc_prepeak"]
DSOC_PEAK_KEYS = ["delta_soc_peak", "dsoc_peak"]
GATE_KEYS = ["mechanism_gate_pass", "gate_pass"]
```

**P1c. 单步 wall-clock 估算**：从 dryrun_w2_milp_96 的 result.json / stdout 读 mean solve time。如果 > 0.15 s/step → 全年估时 > 90 min，提示用户但不停。

**PHASE 1 期望产出**：3 个 dry-run runs 目录；任一 dry-run 失败 → 停下贴 traceback。

---

### PHASE 2 — 全年串行 batch（~75-105 min）

**P2a. 写 `tools/run_w2_scenario_batch.ps1`**：

```powershell
$ErrorActionPreference = "Stop"
$Python = "D:/Anaconda/python.exe"
$OutDir = "runs/m2_tes_mpc_oracle"
$TS = Get-Date -Format "yyyyMMdd_HHmmss"

function Run-Cmd {
    param([string]$Tag, [string[]]$Args)
    Write-Host "==> $Tag"
    $startTime = Get-Date
    & $Python @Args
    $exit = $LASTEXITCODE
    $duration = (Get-Date) - $startTime
    Write-Host "    exit=$exit duration=$($duration.TotalMinutes.ToString('F1')) min"
    if ($exit -ne 0) { throw "cell $Tag failed (exit $exit)" }
}

# Cell 1: MPC-MILP full year
Run-Cmd "w2_mpc_milp_year_$TS" @(
    "tools/m2_tes_mpc_oracle.py",
    "--tag", "w2_mpc_milp_year_$TS",
    "--eval-design", "trainlike",
    "--out-dir", $OutDir,
    "--solver", "milp",
    "--forecast-noise-mode", "perfect"
)

# Cell 2: MPC-Heuristic full year
Run-Cmd "w2_mpc_heuristic_year_$TS" @(
    "tools/m2_tes_mpc_oracle.py",
    "--tag", "w2_mpc_heuristic_year_$TS",
    "--eval-design", "trainlike",
    "--out-dir", $OutDir,
    "--solver", "heuristic",
    "--forecast-noise-mode", "perfect"
)

# Cell 3: no-TES baseline (policy=neutral, valve永远=0)
Run-Cmd "w2_baseline_neutral_year_$TS" @(
    "tools/evaluate_m2_rule_baseline.py",
    "--tag", "w2_baseline_neutral_year_$TS",
    "--eval-design", "trainlike",
    "--policy", "neutral"
)

Write-Host "TS=$TS"
Write-Host "Batch OK, 3 full-year cells, no failures."
$TS | Out-File -Encoding utf8 tools/_w2_batch_ts.txt
```

**P2b. 执行**（前台串行，不背景；一次跑完顺序进 PHASE 3 不切走）：

```powershell
powershell -ExecutionPolicy Bypass -File tools/run_w2_scenario_batch.ps1
```

**P2c. 任一 cell 失败** → set -e 自动停 → 贴 stdout 末 50 行 + 失败 cell 的 workspace 目录 + result.json 是否存在 + monitor.csv 行数。

**P2d. 全部成功后**：
- 读 `tools/_w2_batch_ts.txt` 拿到 TS（PHASE 3 用）
- 验证 3 个 monitor.csv 都接近 35041 行（35040 + header）。任一 < 35000 → 停下贴行数。

---

### PHASE 3 — 聚合脚本 + 跑

**P3a. 写 `tools/build_w2_scenario_summary.py`**（完整代码，照抄）：

```python
"""W2 scenario summary: TES energy + PV self-consumption.

Reads 3 cells (MILP / Heuristic / Baseline-neutral) and aggregates:
- §4.1 energy: cost_usd_total, total_load_mwh, pue_avg, comfort_violation_pct
- §4.2 PV   : self_consumption_rate, pv_load_coverage, grid_import/export
- MPC-only  : sign_rate, dsoc_prepeak/peak, mode_switches, gate_pass

Baseline cell columns: filled with N/A for MPC-only metrics.
PV column auto-detected via aliases. If baseline lacks PV column (PV wrapper
not loaded), fall back to reconstructing from PV CSV + monitor timestamp.
"""
from __future__ import annotations

import argparse
import json
from pathlib import Path
from typing import Any

import numpy as np
import pandas as pd


PV_ALIASES = ["current_pv_kw", "pv_current_kw", "pv_kw", "pv_power_kw"]
FACILITY_ALIASES = ["Electricity:Facility", "Facility:Electricity", "Electricity_Facility"]
IT_ALIASES = ["ITE-CPU:InteriorEquipment:Electricity", "ITE_CPU_InteriorEquipment_Electricity"]
SOC_ALIASES = ["TES_SOC", "tes_soc"]
MODE_ALIASES = ["tes_mpc_mode_label"]

COST_KEYS = ["cost_usd", "cost_usd_total", "total_cost_usd", "cost_total"]
PUE_KEYS = ["pue", "pue_avg", "PUE"]
COMFORT_KEYS = ["comfort_violation_pct", "comfort_pct", "comfort"]
SIGN_RATE_KEYS = ["charge_window_sign_rate"]
DSOC_PREPEAK_KEYS = ["delta_soc_prepeak", "dsoc_prepeak"]
DSOC_PEAK_KEYS = ["delta_soc_peak", "dsoc_peak"]
GATE_KEYS = ["mechanism_gate_pass", "gate_pass"]


def first_match(d: dict, keys: list[str], default=None):
    for k in keys:
        if k in d and d[k] is not None:
            return d[k]
    return default


def first_col(df: pd.DataFrame, aliases: list[str]) -> str | None:
    for c in aliases:
        if c in df.columns:
            return c
    return None


def load_cell(run_dir: Path) -> tuple[pd.DataFrame, dict]:
    monitor_path = run_dir / "monitor.csv"
    result_path = run_dir / "result.json"
    if not monitor_path.exists():
        raise FileNotFoundError(f"missing monitor.csv: {monitor_path}")
    df = pd.read_csv(monitor_path)
    result = {}
    if result_path.exists():
        with open(result_path, "r", encoding="utf-8") as f:
            result = json.load(f)
        if isinstance(result, list) and result:
            result = result[0]
    return df, result


def reconstruct_pv_kw(df: pd.DataFrame, pv_csv: Path, peak_kw: float = 6000.0) -> np.ndarray:
    """Fallback: rebuild PV series from CSV + step index. Assumes step starts at
    Jan 1, 0:00 and timesteps_per_hour=4 (M2 standard)."""
    pv = pd.read_csv(pv_csv)
    if "power_kw" in pv.columns:
        hourly_kw = pv["power_kw"].to_numpy(dtype=np.float64)
    elif "power_ratio" in pv.columns:
        hourly_kw = pv["power_ratio"].to_numpy(dtype=np.float64) * peak_kw
    else:
        raise RuntimeError(f"PV CSV {pv_csv} missing power_kw / power_ratio column")
    n_rows = len(df)
    upsampled = np.repeat(hourly_kw, 4)
    if len(upsampled) < n_rows:
        upsampled = np.tile(upsampled, int(np.ceil(n_rows / len(upsampled))))
    return upsampled[:n_rows]


def derive_pv_metrics(
    df: pd.DataFrame,
    pv_csv: Path,
    timesteps_per_hour: int = 4,
) -> dict[str, float]:
    pv_col = first_col(df, PV_ALIASES)
    fac_col = first_col(df, FACILITY_ALIASES)
    if fac_col is None:
        raise RuntimeError(f"facility column not found. monitor cols={list(df.columns)[:30]}...")

    if pv_col is not None:
        pv_kw = df[pv_col].astype(float).to_numpy()
    else:
        print(f"  [info] PV column missing in monitor; reconstructing from {pv_csv}")
        pv_kw = reconstruct_pv_kw(df, pv_csv)

    fac_raw = df[fac_col].astype(float).to_numpy()
    median = float(np.median(np.abs(fac_raw)))
    if median > 1e5:
        step_seconds = 3600.0 / timesteps_per_hour
        load_kw = fac_raw / step_seconds / 1000.0
    else:
        load_kw = fac_raw / 1000.0

    pv_consumed_kw = np.minimum(pv_kw, load_kw)
    dt_h = 1.0 / timesteps_per_hour

    pv_total_gen_mwh = float(pv_kw.sum() * dt_h / 1000.0)
    pv_consumed_mwh = float(pv_consumed_kw.sum() * dt_h / 1000.0)
    load_total_mwh = float(load_kw.sum() * dt_h / 1000.0)

    if pv_total_gen_mwh > 0:
        scr_pct = pv_consumed_mwh / pv_total_gen_mwh * 100.0
    else:
        scr_pct = 0.0
    if load_total_mwh > 0:
        coverage_pct = pv_consumed_mwh / load_total_mwh * 100.0
    else:
        coverage_pct = 0.0

    grid_import_mwh = float(np.maximum(0, load_kw - pv_kw).sum() * dt_h / 1000.0)
    grid_export_mwh = float(np.maximum(0, pv_kw - load_kw).sum() * dt_h / 1000.0)

    return {
        "pv_total_gen_mwh": pv_total_gen_mwh,
        "pv_consumed_mwh": pv_consumed_mwh,
        "self_consumption_rate_pct": scr_pct,
        "pv_load_coverage_pct": coverage_pct,
        "grid_import_mwh": grid_import_mwh,
        "grid_export_mwh": grid_export_mwh,
        "total_load_mwh": load_total_mwh,
    }


def derive_mpc_metrics(df: pd.DataFrame, result: dict) -> dict[str, Any]:
    """Sign rate / dsoc / mode_switches; falls back to N/A if not MPC cell."""
    out = {
        "sign_rate": first_match(result, SIGN_RATE_KEYS, "N/A"),
        "dsoc_prepeak": first_match(result, DSOC_PREPEAK_KEYS, "N/A"),
        "dsoc_peak": first_match(result, DSOC_PEAK_KEYS, "N/A"),
        "mechanism_gate_pass": first_match(result, GATE_KEYS, "N/A"),
    }
    mode_col = first_col(df, MODE_ALIASES)
    if mode_col:
        labels = df[mode_col].fillna("").astype(str).to_numpy()
        out["mode_switches"] = int(np.sum(labels[1:] != labels[:-1]))
    else:
        out["mode_switches"] = "N/A"
    return out


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--ts", required=True)
    ap.add_argument("--runs-dir", default="runs/m2_tes_mpc_oracle")
    ap.add_argument("--baseline-dir", default=None,
                    help="If baseline output not in runs-dir, override here.")
    ap.add_argument("--pv-csv", default="Data/pv/CHN_Nanjing_PV_6MWp_hourly.csv")
    args = ap.parse_args()

    runs_dir = Path(args.runs_dir)
    cells_spec = [
        ("baseline_neutral", f"w2_baseline_neutral_year_{args.ts}", args.baseline_dir),
        ("heuristic", f"w2_mpc_heuristic_year_{args.ts}", None),
        ("mpc_milp", f"w2_mpc_milp_year_{args.ts}", None),
    ]

    rows = []
    for algo, tag, override_dir in cells_spec:
        run_dir = Path(override_dir) / tag if override_dir else runs_dir / tag
        if not (run_dir / "monitor.csv").exists():
            candidates = list(Path("runs").rglob(f"*{tag}*/monitor.csv"))
            candidates += list(Path("runs").rglob(f"*{tag}*/episode-001/monitor.csv"))
            if candidates:
                run_dir = candidates[0].parent
                print(f"  [info] {algo}: located monitor at {run_dir}")
            else:
                raise FileNotFoundError(f"cannot locate monitor.csv for tag={tag}")

        print(f"==> {algo} ({tag})")
        df, result = load_cell(run_dir)
        print(f"    rows={len(df)} cols={len(df.columns)} result_keys={len(result)}")

        cost_total = first_match(result, COST_KEYS)
        if cost_total is None:
            if "cost_usd_step" in df.columns:
                cost_total = float(df["cost_usd_step"].astype(float).sum())
            else:
                cost_total = float("nan")
        pue = first_match(result, PUE_KEYS, float("nan"))
        comfort = first_match(result, COMFORT_KEYS, float("nan"))

        pv = derive_pv_metrics(df, Path(args.pv_csv))
        mpc = derive_mpc_metrics(df, result)

        row = {
            "algorithm": algo,
            "tag": tag,
            "total_steps": len(df),
            "cost_usd_total": cost_total,
            "total_load_mwh": pv["total_load_mwh"],
            "pue_avg": pue,
            "comfort_violation_pct": comfort,
            "pv_total_gen_mwh": pv["pv_total_gen_mwh"],
            "pv_consumed_mwh": pv["pv_consumed_mwh"],
            "self_consumption_rate_pct": pv["self_consumption_rate_pct"],
            "pv_load_coverage_pct": pv["pv_load_coverage_pct"],
            "grid_import_mwh": pv["grid_import_mwh"],
            "grid_export_mwh": pv["grid_export_mwh"],
            **mpc,
        }
        rows.append(row)

    df_out = pd.DataFrame(rows)

    base = df_out[df_out["algorithm"] == "baseline_neutral"].iloc[0]
    base_cost = float(base["cost_usd_total"])
    base_pue = float(base["pue_avg"])
    base_scr = float(base["self_consumption_rate_pct"])

    df_out["cost_saving_vs_baseline_usd"] = base_cost - df_out["cost_usd_total"].astype(float)
    df_out["cost_saving_vs_baseline_pct"] = (
        df_out["cost_saving_vs_baseline_usd"] / base_cost * 100.0
    )
    df_out["pue_improvement_vs_baseline"] = base_pue - df_out["pue_avg"].astype(float)
    df_out["self_consumption_uplift_vs_baseline_pp"] = (
        df_out["self_consumption_rate_pct"].astype(float) - base_scr
    )

    out_csv = Path(f"analysis/m2f1_w2_scenario_compare_{args.ts}.csv")
    out_md = Path(f"analysis/m2f1_w2_scenario_compare_{args.ts}.md")
    df_out.to_csv(out_csv, index=False)

    energy_cols = ["algorithm", "total_steps", "cost_usd_total", "total_load_mwh",
                   "pue_avg", "comfort_violation_pct",
                   "cost_saving_vs_baseline_usd", "cost_saving_vs_baseline_pct",
                   "pue_improvement_vs_baseline"]
    pv_cols = ["algorithm", "pv_total_gen_mwh", "pv_consumed_mwh",
               "self_consumption_rate_pct", "pv_load_coverage_pct",
               "grid_import_mwh", "grid_export_mwh",
               "self_consumption_uplift_vs_baseline_pp"]
    mpc_cols = ["algorithm", "sign_rate", "dsoc_prepeak", "dsoc_peak",
                "mode_switches", "mechanism_gate_pass"]

    with open(out_md, "w", encoding="utf-8") as f:
        f.write(f"# W2 Scenario Comparison ({args.ts})\n\n")
        f.write("## §4.1 TES 节能贡献\n\n")
        f.write(df_out[energy_cols].to_markdown(index=False, floatfmt=".4f"))
        f.write("\n\n## §4.2 PV 自消纳对照\n\n")
        f.write(df_out[pv_cols].to_markdown(index=False, floatfmt=".4f"))
        f.write("\n\n## MPC 机理诊断（仅 MPC cell）\n\n")
        f.write(df_out[mpc_cols].to_markdown(index=False))
        f.write("\n")

    print(f"\nWrote {out_csv}")
    print(f"Wrote {out_md}")
    print("\n=== ENERGY TABLE ===")
    print(df_out[energy_cols].to_string(index=False))
    print("\n=== PV TABLE ===")
    print(df_out[pv_cols].to_string(index=False))


if __name__ == "__main__":
    main()
```

**P3b. 跑**：

```powershell
$TS = Get-Content -Encoding utf8 tools/_w2_batch_ts.txt | Select-Object -First 1
& D:/Anaconda/python.exe tools/build_w2_scenario_summary.py --ts $TS
```

**P3c. 验收**（自动检查）：
- CSV 3 行齐
- `cost_baseline ≥ cost_heuristic ≥ cost_milp`（节能单调性）
- `scr_baseline ≤ scr_heuristic ≤ scr_milp` 或者 scr 三 cell 接近（差距 < 2 pp）—— scr 反向 → 停下贴诊断
- cost 反向 → 停下贴诊断
- comfort 三 cell < 5%

---

### PHASE 4 — commit + push

```bash
git add tools/run_w2_scenario_batch.ps1 tools/build_w2_scenario_summary.py \
        analysis/m2f1_w2_scenario_compare_${TS}.csv \
        analysis/m2f1_w2_scenario_compare_${TS}.md
```

```bash
git commit -m "feat(mpc): w2 scenario batch (TES energy + PV self-consumption, full year trainlike)

Cells: 3 algorithms × 35040 step trainlike full year.
- w2_mpc_milp_year_<ts>          : MILP 24h rolling horizon
- w2_mpc_heuristic_year_<ts>     : rule-based with TES (heuristic policy)
- w2_baseline_neutral_year_<ts>  : no-TES baseline (evaluate_m2_rule_baseline.py
                                    --policy neutral, valve永远=0)

All 3 cells share identical wrapper chain (PVSignal + PriceSignal + TESValve),
trainlike eval-design (DRL_DC_training.epJSON, ITE_Set=0.45), and 4-step/h
resolution — directly comparable.

Aggregator computes:
- §4.1 energy: cost_usd_total, total_load_mwh, pue_avg, comfort_violation_pct
- §4.2 PV   : pv_consumed_mwh, self_consumption_rate, pv_load_coverage,
              grid_import/export, self_consumption_uplift_vs_baseline_pp
- MPC-only  : sign_rate, dsoc_prepeak/peak, mode_switches, gate_pass

Output: analysis/m2f1_w2_scenario_compare_<ts>.{csv,md}.
Wall-clock: ~75-105 min (MILP-dominated).
"
git push origin master
```

---

## 最终回贴清单

W2 完成后回贴：

1. commit hash
2. 3 个 runs 路径
3. `analysis/m2f1_w2_scenario_compare_<ts>.md` 内容（三张表）
4. 关键诊断（如有）：
   - 任一 cell 的 fallback 步数（MPC 内部 LP 不收敛回退）
   - PV 重建是否触发（baseline cell 是否走了 reconstruct_pv_kw）
   - 单调性是否符合预期
5. 总 wall-clock

**不要总结/解读结论；那是用户看到数据后再聊的事。**

---

## 验证（plan 自检，不需跑代码即可确认）

| 检查 | 期望 |
|---|---|
| 每 cell monitor.csv 行数 | ≈ 35041 |
| result.json 关键字段非空非 NaN | 每 cell |
| 聚合表 3 行齐 | baseline_neutral / heuristic / mpc_milp |
| 节能单调性 | `cost_baseline ≥ cost_heuristic ≥ cost_milp` |
| PV 自消纳单调性 | `scr_baseline ≤ scr_heuristic ≤ scr_milp` |
| PV 自消纳上限 | scr 不超过 ~67% |
| Comfort | 三 cell 都 < 5% |
| MPC vs baseline cost saving | 数量级 5–15% |
| MPC vs baseline scr uplift | 数量级 5–15 pp |

---

## 已知风险与应对

| 风险 | 概率 | 应对 |
|---|---|---|
| MILP 全年某些 step LP 不收敛 → fallback to heuristic | 中（W1-3 σ=0.20 已观测）| W1-2 fallback 已就位；聚合表标 fallback step count |
| baseline monitor.csv 缺 `current_pv_kw` 列（PV wrapper 未挂）| 低（evaluate_m2_rule_baseline.py 已挂 PVSignalWrapper）| `derive_pv_metrics` 自带 fallback：从 PV CSV 重建 |
| MPC vs Heuristic 节能差距 < 1% | 中-高 | 不视为失败；论文 §4.1 重心放在「TES + 任意控制 vs no-TES」工程价值 |
| PV 自消纳率三 cell 差距 < 2 pp | 中 | 检查 wrapper 注入 + ITE_Set / fan 限制白天负载消纳能力 |
| 全年 wall-clock > 90 min | 中 | 串行跑，等待即可 |

---

## 不在本 plan 范围（W3 或更后）

- W2-A LP × 9 gaussian cells 对照（W1 §4.1 二次曲线）— 推迟
- M2-G1 BC label 生成 / M2-G2 BC warm-start / M2-G3 final eval — 看 W2 数据效果再定
- closed-loop deploy 验证 — 单独 plan
- 多周场景切片 / 多站点泛化 — 看 W2 数据效果再定
